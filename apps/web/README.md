# @announcing/web

SvelteKit app for Announcing, deployed to Cloudflare Workers.

Part of the [Announcing monorepo](../../README.md) — run commands from the repo
root (`pnpm dev`, `pnpm build`, …). App-specific config lives in `wrangler.jsonc`
and `vite.config.ts`.
